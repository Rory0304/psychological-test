const laptopSmaller = 'only screen and (max-width:1024px)';
const tabletSmaller = 'only screen and (max-width:768px)';
const mobileSmaller = 'only screen and (max-width:640px)';

const breakpoints = Object.freeze({
  laptopSmaller,
  tabletSmaller,
  mobileSmaller,
});

export default breakpoints;
